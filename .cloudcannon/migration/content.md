# Content migration

No verified business copy was changed. Astro imports the six existing HTML source documents at build time and emits the same routes.

The content extraction required for a page-builder collection remains a deliberately separate task: it needs schemas for the home, about, training, booking, contact, and media section types, plus a shared-data model for navigation and footer content.
